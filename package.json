{
  "name": "mcp-server",
  "version": "2.0.2",
  "description": "MCP server with sudo-aware system file editing tools (exec, write_file, write_file_sudo, read_file with line-range, stat_file, str_replace). Deployed on both Hetzner (public) and Lenovo1 (internal), distinguished by env var MCP_SERVER_NAME.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
